// ASSETS
import chartIcon from '../../assets/icons/shopping-cart.png';

const CartWidget = () => {
    return (
        <img
        src={chartIcon}
        alt="chart icon"
        width="25px"
        />
    );
};

export default CartWidget;